# Templates

This directory holds report templates. The current report generators in
`vra/reporting/` build HTML, Markdown, and JSON output directly in Python
(no external template engine at runtime).

If you want to customize the HTML report, edit the template literal inside
`vra/reporting/html.py`.
